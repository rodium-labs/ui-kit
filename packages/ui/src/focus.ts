// one ring for the whole kit. no radius: border-radius rounds the control, and
// the outline only follows it, so a rounded ring means a rounded button.
export const focus =
  'focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-(--color-brand-green)'
